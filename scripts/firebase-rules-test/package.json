{
  "name": "sylyo-firebase-rules-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Emulator-based tests for Sylyo's Firestore & Storage security rules.",
  "scripts": {
    "test": "cd ../.. && firebase emulators:exec --project demo-sylyo --only firestore,storage \"node scripts/firebase-rules-test/rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.3.0"
  }
}
