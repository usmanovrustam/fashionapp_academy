# Runtime deps for run_cutout.py (works on any recent Python, incl. 3.13).
# On macOS just use plain PyPI (no --index-url needed):
#   pip install -r requirements.txt
torch
numpy
pillow
huggingface_hub
