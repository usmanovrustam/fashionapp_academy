# CPU wheels: install torch/torchvision from the PyTorch CPU index, e.g.
#   pip install --index-url https://download.pytorch.org/whl/cpu torch==2.5.1 torchvision==0.20.1
#   pip install -r requirements.txt
# (torch 2.5.1 + coremltools 8.3.0 match the toolchain that produced u2net.mlpackage.)
torch==2.5.1
torchvision==0.20.1
coremltools==8.3.0
datasets>=3.0
scikit-learn
pandas
pyarrow
numpy
tqdm
